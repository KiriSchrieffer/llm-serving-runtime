# Scheduler comparison config — FIFO vs Priority
base_url: http://llm-runtime:8000
concurrency_levels: [4, 16]
requests_per_level: 32
max_tokens: 16
schedulers: [fifo, priority]
output: benchmarks/results/scheduler.json